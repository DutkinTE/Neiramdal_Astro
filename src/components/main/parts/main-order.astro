
<div class="content">
  <div class="title reveal-type">
    Готовы реализовать<br />проект вместе с нами?
  </div>
  <div class="subtitle reveal-type">Заполните заявку на сайте!</div>
</div>
<a href="/order">
  

  <button class="button hover"> Оставить заявку </button>
</a>

<script>
  import Lenis from "lenis";

  import "lenis/dist/lenis.css";
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";
  import SplitType from "split-type";
  gsap.registerPlugin(ScrollTrigger);

  const splitTypes = document.querySelectorAll(".reveal-type");

  splitTypes.forEach((char, i) => {
    const bg = "transparent";
    const fg = "#f5f5f5";

    const text = new SplitType(char as HTMLElement, { types: "chars" });

    gsap.fromTo(
      text.chars,
      { color: bg },
      {
        color: fg,
        duration: 0.3,
        stagger: 0.02,
        scrollTrigger: {
          trigger: char,
          start: "top 77%",
          end: "top 50%",
          scrub: true,
          markers: false,
          onLeaveBack: () => gsap.to(text.chars, { color: bg, duration: 0.3 }),
        },
      }
    );
  });

  const lenis = new Lenis();

  function raf(time) {
    lenis.raf(time);
    requestAnimationFrame(raf);
  }

  requestAnimationFrame(raf);
</script>

<style>
  .content {
    text-align: center;
  }
  .title {
    font-size: 40px;
    font-weight: bold;
  }
  .subtitle {
    font-size: 24px;
    margin-top: 20px;
    margin-bottom: 80px;
  }

  .button {
    font-family: "Play", serif;
    position: relative;
    border: 1px solid #f5f5f5;
    background-color: transparent;
    color: #f5f5f5;
    font-size: 20px;
    width: 300px;
    height: 70px;
    font-weight: bold;
    border-radius: 30px;
    cursor: pointer;
    overflow: hidden;
    z-index: 1;
    transition:
      color 0.3s ease,
      border-color 0.3s,
      border 0.3s ease;
  }

  .button::after {
    content: "";
    position: absolute;
    bottom: -400px;
    left: -46px;
    width: 400px;
    height: 400px;
    background-color: #f5f5f5;
    border-radius: 100%;
    overflow: hidden;
    transition: all 0.5s;
    z-index: -1;
  }

  .button:hover::after {
    bottom: -260px;
  }

  .button:hover {
    cursor: none;
    color: #050505;
    border-color: #050505;
  }

  .button:active {
    color: #7855ed;
    border: 1px solid #7855ed;
  }
</style>
